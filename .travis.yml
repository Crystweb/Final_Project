dist: trusty

language: java

jdk:
  - oraclejdk8

cache:
  directories:
    - $HOME/.m2
    - ./client/node_modules/

script:
  - mvn clean test jacoco:report coveralls:report
  - npm run build --prefix client/

before_script:
  - npm install --prefix client/


